# Input Data

This directory contains eye-tracking data collected from subjects. Due to size constraints, the data files are not included in this repository.

The data structure includes:
- CSV files with eye-tracking coordinates
- MP4 video files of reading sessions
- Various other eye-tracking metrics

Each participant's data is stored in a subdirectory named by their identifier. 